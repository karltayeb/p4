/** returns distance. 
 *  @param <T> t. */
public interface Distance<T> {
    /** returns distance. 
 *      @param one one.
 *      @param two two. 
 *      @return double distance. */
    double distance(T one, T two);
}
